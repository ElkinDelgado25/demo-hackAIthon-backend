name: Keep Render Backend Awake

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping backend
        run: |
          echo "Pinging Render backend..."
          curl -L --max-time 60 -sS https://demo-hackaithon-backend.onrender.com/ || true
          echo "Done"
